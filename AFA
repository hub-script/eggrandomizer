local scriptContent = [[
local players = game:GetService("Players")
local collectionService = game:GetService("CollectionService")
local localPlayer = players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local StarterGui = game:GetService("StarterGui")

-- Debugging print to confirm script execution start
print("Pet Randomizer Script: Starting execution.")

-- Ensure LocalPlayer is available. Use game.Players.LocalPlayer first, as it's the standard.
if not localPlayer then
    localPlayer = players:GetPlayers()[1] -- Fallback for some non-standard environments
    if not localPlayer then
        warn("Pet Randomizer Script: ERROR - Could not get LocalPlayer. GUI will not appear.")
        return -- Exit if LocalPlayer is not found, as the script cannot function without it
    end
end
print("Pet Randomizer Script: LocalPlayer obtained:", localPlayer.Name)

-- Egg chances data (from your original script)
local eggChances = {
    ["Common Egg"] = {["Dog"] = 33, ["Bunny"] = 33, ["Golden Lab"] = 33},
    ["Uncommon Egg"] = {["Black Bunny"] = 25, ["Chicken"] = 25, ["Cat"] = 25, ["Deer"] = 25},
    ["Rare Egg"] = {["Orange Tabby"] = 33.33, ["Spotted Deer"] = 25, ["Pig"] = 16.67, ["Rooster"] = 16.67, ["Monkey"] = 8.33},
    ["Legendary Egg"] = {["Cow"] = 42.55, ["Silver Monkey"] = 42.55, ["Sea Otter"] = 10.64, ["Turtle"] = 2.13, ["Polar Bear"] = 2.13},
    ["Mythic Egg"] = {["Grey Mouse"] = 37.5, ["Brown Mouse"] = 26.79, ["Squirrel"] = 26.79, ["Red Giant Ant"] = 8.93, ["Red Fox"] = 0},
    ["Bug Egg"] = {["Snail"] = 40, ["Giant Ant"] = 35, ["Caterpillar"] = 25, ["Praying Mantis"] = 0, ["Dragon Fly"] = 0},
    ["Night Egg"] = {["Hedgehog"] = 47, ["Mole"] = 23.5, ["Frog"] = 21.16, ["Echo Frog"] = 8.35, ["Night Owl"] = 0, ["Raccoon"] = 0},
    ["Bee Egg"] = {["Bee"] = 65, ["Honey Bee"] = 20, ["Bear Bee"] = 10, ["Petal Bee"] = 5, ["Queen Bee"] = 0},
    ["Anti Bee Egg"] = {["Wasp"] = 55, ["Tarantula Hawk"] = 31, ["Moth"] = 14, ["Butterfly"] = 0, ["Disco Bee"] = 0},
    ["Common Summer Egg"] = {["Starfish"] = 50, ["Seafull"] = 25, ["Crab"] = 25},
    ["Rare Summer Egg"] = {["Flamingo"] = 30, ["Toucan"] = 25, ["Sea Turtle"] = 20, ["Orangutan"] = 15, ["Seal"] = 10},
    ["Paradise Egg"] = {["Ostrich"] = 43, ["Peacock"] = 33, ["Capybara"] = 24, ["Scarlet Macaw"] = 3, ["Mimic Octopus"] = 1},
    ["Premium Night Egg"] = {["Hedgehog"] = 50, ["Mole"] = 26, ["Frog"] = 14, ["Echo Frog"] = 10},
    ["Dinosaur Egg"] = {["Raptor"] = 33, ["Triceratops"] = 33, ["T-Rex"] = 1, ["Stegosaurus"] = 33, ["Pterodactyl"] = 33, ["Brontosaurus"] = 33}
}

-- Eggs for which ESP is considered "real"
local realESP = {
    ["Common Egg"] = true, ["Uncommon Egg"] = true, ["Rare Egg"] = true,
    ["Common Summer Egg"] = true, ["Rare Summer Egg"] = true
}

-- Auto Stop Pets List (REMOVED: No longer used as auto-stop feature is removed)
-- local autoStopPets = {
--     ["Raccoon"] = true, ["Dragonfly"] = true, ["Queen Bee"] = true,
--     ["Red Fox"] = true, ["Disco Bee"] = true, ["Butterfly"] = true, ["Fennec Fox"] = true
-- }

local displayedEggs = {} -- Stores information about currently displayed ESPs
-- local autoStopOn = true -- REMOVED: Auto-stop feature removed
local espOn = true -- State for ESP display

-- Function to pick a random pet based on weighted chances
local function weightedRandom(options)
    local valid = {}
    for pet, chance in pairs(options) do
        if chance > 0 then table.insert(valid, {pet = pet, chance = chance}) end
    end
    if #valid == 0 then return nil end
    local total = 0
    for _, v in ipairs(valid) do total += v.chance end
    local roll = math.random() * total
    local cumulative = 0
    for _, v in ipairs(valid) do
        cumulative += v.chance
        if roll <= cumulative then return v.pet end
    end
    return valid[1].pet -- Fallback in case of floating point inaccuracies
end

-- Function to get a non-repeating random pet (STRICTLY non-repeating unless no other options)
local function getNonRepeatingRandomPet(eggName, lastPet)
    local pool = eggChances[eggName]
    if not pool then return nil end

    local availablePets = {}
    for pet, chance in pairs(pool) do
        if pet ~= lastPet and chance > 0 then -- Only add if different from lastPet and has chance > 0
            table.insert(availablePets, {pet = pet, chance = chance})
        end
    end

    if #availablePets == 0 then
        -- If no other pets are available, or only the last pet is available,
        -- then we have to pick the last pet or nil if pool is empty/invalid.
        return weightedRandom(pool) -- Fallback to any pet from the original pool
    else
        -- Otherwise, pick from the available (non-repeating) pets
        local total = 0
        for _, v in ipairs(availablePets) do total += v.chance end
        local roll = math.random() * total
        local cumulative = 0
        for _, v in ipairs(availablePets) do
            cumulative += v.chance
            if roll <= cumulative then return v.pet end
        end
        return availablePets[1].pet -- Fallback
    end
end

-- Function to create the BillboardGui for ESP
local function createEspGui(object, labelText)
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "FakePetESP"
    -- Adornee to the primary part or the object itself
    billboard.Adornee = object:FindFirstChildWhichIsA("BasePart") or object.PrimaryPart or object
    billboard.Size = UDim2.new(0, 200, 0, 50)
    billboard.StudsOffset = Vector3.new(0, 2.5, 0)
    billboard.AlwaysOnTop = true
    billboard.Enabled = true -- Ensure it's enabled by default

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.new(1, 1, 1)
    label.TextStrokeTransparency = 0
    label.TextScaled = true
    label.Font = Enum.Font.SourceSansBold
    label.Text = labelText
    label.Parent = billboard

    billboard.Parent = object
    print("Pet Randomizer Script: Created ESP for", object.Name, "with text:", labelText)
    return billboard
end

-- Function to add ESP to an egg
local function addESP(egg)
    if not espOn then
        print("Pet Randomizer Script: ESP is OFF, not adding for", egg.Name)
        return
    end
    -- Only show ESP for eggs owned by the local player
    if egg:GetAttribute("OWNER") ~= localPlayer.Name then return end
    local eggName = egg:GetAttribute("EggName")
    local objectId = egg:GetAttribute("OBJECT_UUID")
    if not eggName or not objectId then
        warn("Pet Randomizer Script: Missing EggName or OBJECT_UUID for egg:", egg.Name)
        return
    end
    if displayedEggs[objectId] then
        print("Pet Randomizer Script: ESP already exists for", egg.Name, "(ID:", objectId, ")")
        return
    end

    local labelText, firstPet
    if realESP[eggName] then
        labelText = eggName
    else
        firstPet = getNonRepeatingRandomPet(eggName, nil)
        labelText = eggName .. " | " .. (firstPet or "?")
    end

    local espGui = createEspGui(egg, labelText)
    displayedEggs[objectId] = {
        egg = egg,
        gui = espGui,
        label = espGui:FindFirstChild("TextLabel"),
        eggName = eggName,
        lastPet = firstPet
    }
    print("Pet Randomizer Script: Added ESP for egg:", egg.Name, "ID:", objectId)
end

-- Function to remove ESP from an egg
local function removeESP(egg)
    local objectId = egg:GetAttribute("OBJECT_UUID")
    if objectId and displayedEggs[objectId] then
        displayedEggs[objectId].gui:Destroy()
        displayedEggs[objectId] = nil
        print("Pet Randomizer Script: Removed ESP for egg ID:", objectId)
    end
end

-- Initial setup for existing eggs
for _, egg in collectionService:GetTagged("PetEggServer") do
    addESP(egg)
end

-- Connect to signals for new and removed eggs
collectionService:GetInstanceAddedSignal("PetEggServer"):Connect(addESP)
collectionService:GetInstanceRemovedSignal("PetEggServer"):Connect(removeESP)

-- Create the main ScreenGui
local gui = Instance.new("ScreenGui")
gui.Name = "PetRandomizerGUI"
gui.ResetOnSpawn = false -- Important for persistent GUI across spawns
gui.Archivable = true -- Ensure it can be archived/replicated if needed (good practice)

-- AGGRESSIVE GUI PARENTING LOGIC
local function waitForPlayerGui()
    local tries = 0
    while tries < 10 do -- Try for up to 10 seconds
        local playerGui = localPlayer:FindFirstChild("PlayerGui")
        if playerGui then return playerGui end
        tries += 1
        task.wait(1) -- Wait 1 second between tries
    end
    return nil -- Return nil if PlayerGui is not found after all tries
end

local playerGui = waitForPlayerGui()

if playerGui then
    gui.Parent = playerGui
    print("✅ Pet Randomizer Script: GUI successfully parented to PlayerGui.")
else
    warn("❌ Pet Randomizer Script: GUI NOT parented - PlayerGui not found after retrying.")
end

-- Debug Immediately: Check GUI visibility after a short delay
task.delay(2, function()
    if gui and gui.Parent then
        warn("✅ Pet Randomizer Script: GUI visible in", gui.Parent:GetFullName())
    else
        warn("❌ Pet Randomizer Script: GUI still not visible or parented after 2 seconds.")
    end
end)

-- Create the main frame for the GUI
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 300, 0, 250) -- Adjusted size to fit content
mainFrame.Position = UDim2.new(0.5, -150, 0.5, -125) -- Center the GUI
mainFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
mainFrame.BorderSizePixel = 0
-- mainFrame.CornerRadius = UDim.new(0, 8) -- REMOVED: Causing "not a valid member" error
mainFrame.Parent = gui
mainFrame.Active = true -- Make the frame active to receive input for dragging

-- Store original size for minimize/maximize
local originalFrameSize = mainFrame.Size
local minimizedFrameSize = UDim2.new(0, 300, 0, 40) -- Height of just the title bar
local isMinimized = false

-- Add a UIListLayout for vertical arrangement of elements
local listLayout = Instance.new("UIListLayout")
listLayout.FillDirection = Enum.FillDirection.Vertical
listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
listLayout.VerticalAlignment = Enum.VerticalAlignment.Top
listLayout.Padding = UDim.new(0, 10) -- Padding between elements
listLayout.Parent = mainFrame

-- Add a UIPadding for padding inside the frame
local uiPadding = Instance.new("UIPadding")
uiPadding.PaddingTop = UDim.new(0, 10)
uiPadding.PaddingBottom = UDim.new(0, 10)
uiPadding.PaddingLeft = UDim.new(0, 10)
uiPadding.PaddingRight = UDim.new(0, 10)
uiPadding.Parent = mainFrame

-- Title Bar (contains title and minimize button)
local titleBarFrame = Instance.new("Frame")
titleBarFrame.Name = "TitleBarFrame"
titleBarFrame.Size = UDim2.new(1, 0, 0, 40)
titleBarFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
titleBarFrame.BorderSizePixel = 0
titleBarFrame.Parent = mainFrame
titleBarFrame.LayoutOrder = 1 -- Ensure it's at the top
titleBarFrame.Active = true -- Make the title bar active for dragging

-- Add a horizontal list layout for title and minimize button
local titleBarLayout = Instance.new("UIListLayout")
titleBarLayout.FillDirection = Enum.FillDirection.Horizontal
titleBarLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
titleBarLayout.VerticalAlignment = Enum.VerticalAlignment.Center
titleBarLayout.Padding = UDim.new(0, 5)
titleBarLayout.Parent = titleBarFrame

-- Title Text
local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "TitleLabel"
titleLabel.Size = UDim2.new(1, -45, 1, 0) -- Make space for minimize button
titleLabel.BackgroundTransparency = 1
titleLabel.TextColor3 = Color3.new(1, 1, 1)
titleLabel.TextScaled = true
titleLabel.Font = Enum.Font.SourceSansBold
titleLabel.Text = "👾 Pet Randomizer 👾" -- Matching the image
titleLabel.TextXAlignment = Enum.TextXAlignment.Center
titleLabel.Parent = titleBarFrame

-- Minimize/Maximize Button
local minimizeButton = Instance.new("TextButton")
minimizeButton.Name = "MinimizeButton"
minimizeButton.Size = UDim2.new(0, 30, 0, 30)
minimizeButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
minimizeButton.TextColor3 = Color3.new(1, 1, 1)
minimizeButton.Text = "—" -- Minus sign for minimize
minimizeButton.TextScaled = true
minimizeButton.Font = Enum.Font.SourceSansBold
minimizeButton.BorderSizePixel = 0
minimizeButton.Parent = titleBarFrame

minimizeButton.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    if isMinimized then
        mainFrame.Size = minimizedFrameSize
        minimizeButton.Text = "□" -- Square for maximize
        -- Hide other elements
        madeByLabel.Visible = false
        descLabel.Visible = false
        manualBtn.Visible = false
        espToggleBtn.Visible = false
        infoBtn.Visible = false
        uiPadding.PaddingBottom = UDim.new(0, 0) -- Remove bottom padding when minimized
    else
        mainFrame.Size = originalFrameSize
        minimizeButton.Text = "—" -- Minus sign for minimize
        -- Show other elements
        madeByLabel.Visible = true
        descLabel.Visible = true
        manualBtn.Visible = true
        espToggleBtn.Visible = true
        infoBtn.Visible = true
        uiPadding.PaddingBottom = UDim.new(0, 10) -- Restore bottom padding
    end
end)

-- Make the title bar frame draggable (re-applied to titleBarFrame)
local dragging
local dragInput
local dragStart
local startPos

titleBarFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        input.Handled = true
    end
end)

titleBarFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        if dragging then
            local delta = input.Position - dragStart
            mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end
end)

titleBarFrame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

-- Info Text (Made by Creoleo) - Placed after title bar for correct layout order
local madeByLabel = Instance.new("TextLabel")
madeByLabel.Name = "MadeByLabel"
madeByLabel.Size = UDim2.new(1, 0, 0, 20)
madeByLabel.BackgroundTransparency = 1
madeByLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
madeByLabel.TextScaled = false
madeByLabel.Font = Enum.Font.SourceSans
madeByLabel.TextSize = 14
madeByLabel.Text = "Made by Creoleo"
madeByLabel.Parent = mainFrame
madeByLabel.LayoutOrder = 2

-- Description Text (now includes info from the old info button)
local descLabel = Instance.new("TextLabel")
descLabel.Name = "DescriptionLabel"
descLabel.Size = UDim2.new(1, 0, 0, 80) -- Increased height to accommodate more text
descLabel.BackgroundTransparency = 1
descLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
descLabel.TextScaled = false
descLabel.Font = Enum.Font.SourceSans
descLabel.TextSize = 14
descLabel.TextWrap = true
descLabel.TextXAlignment = Enum.TextXAlignment.Center
descLabel.TextYAlignment = Enum.TextYAlignment.Top
descLabel.Text = "🎲 Randomize pet with a 3-second countdown per reroll.\n\nAuto stop when encountering Raccoon, Dragonfly, Queen Bee, Red Fox, Disco Bee, Butterfly, Fennec Fox."
descLabel.Parent = mainFrame
descLabel.LayoutOrder = 3

-- Function to create a styled button
local function createStyledButton(text, parentFrame, onClickFunction)
    local button = Instance.new("TextButton")
    button.Size = UDim2.new(1, -20, 0, 40) -- Full width minus padding
    button.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    button.TextColor3 = Color3.new(1, 1, 1)
    button.TextScaled = true
    button.Font = Enum.Font.SourceSansBold
    button.Text = text
    button.BorderSizePixel = 0
    button.Parent = parentFrame

    -- Add a UIStroke for a subtle border effect
    local uiStroke = Instance.new("UIStroke")
    uiStroke.Color = Color3.fromRGB(80, 80, 80)
    uiStroke.Thickness = 1
    uiStroke.Parent = button

    button.MouseButton1Click:Connect(onClickFunction)
    return button
end

-- Manual Button (formerly Reroll Pet Display)
local manualBtn = createStyledButton("Manual", mainFrame, function()
    for objectId, data in pairs(displayedEggs) do
        local pet = getNonRepeatingRandomPet(data.eggName, data.lastPet)
        if pet and data.label then
            data.label.Text = data.eggName .. " | " .. pet
            data.lastPet = pet
        end
    end
end)
manualBtn.LayoutOrder = 4

-- ESP Toggle Button
local espToggleBtn = createStyledButton("Esp: ON", mainFrame, function()
    espOn = not espOn
    espToggleBtn.Text = espOn and "Esp: ON" or "Esp: OFF"
    print("Pet Randomizer Script: ESP toggled to:", espOn)
    if espOn then
        print("Pet Randomizer Script: Re-adding ESP for existing eggs.")
        -- Re-add ESP for existing eggs if turned ON
        for _, egg in collectionService:GetTagged("PetEggServer") do
            addESP(egg)
        end
    else
        print("Pet Randomizer Script: Removing all current ESPs.")
        -- Remove all current ESPs if turned OFF
        for objectId, data in pairs(displayedEggs) do
            data.gui:Destroy()
        end
        displayedEggs = {} -- Clear the table
    end
end)
espToggleBtn.LayoutOrder = 5

-- Info Button (REMOVED from separate button, info moved to description)
-- local infoBtn = Instance.new("TextButton")
-- infoBtn.Name = "InfoButton"
-- infoBtn.Size = UDim2.new(0, 30, 0, 30)
-- infoBtn.BackgroundColor3 = Color3.fromRGB(90, 90, 90)
-- infoBtn.TextColor3 = Color3.new(1, 1, 1)
-- infoBtn.Text = "?"
-- infoBtn.TextScaled = true
-- infoBtn.Font = Enum.Font.SourceSansBold
-- infoBtn.BorderSizePixel = 0
-- infoBtn.Parent = mainFrame
-- infoBtn.LayoutOrder = 7
-- infoBtn.MouseButton1Click:Connect(function()
--     pcall(function()
--         StarterGui:SetCore("SendNotification", {
--             Title = "Info",
--             Text = "Auto Stop when found: Raccoon, Dragonfly, Queen Bee, Red Fox, Disco Bee, Butterfly, Fennec Fox.",
--             Duration = 5
--         })
--     end)
-- end)

-- Initial update of button texts
-- autoStopBtn.Text = autoStopOn and "[A] Auto Stop: ON" or "[A] Auto Stop: OFF" -- REMOVED
espToggleBtn.Text = espOn and "Esp: ON" or "Esp: OFF"

print("Pet Randomizer Script: Initialization complete.")
]]

-- Attempt to load and execute the script content
local success, func = pcall(loadstring, scriptContent)

if success and func then
    func()
    print("Loader Script: Successfully loaded and executed the main script.")
else
    warn("Loader Script: ERROR - Failed to load and execute the main script:", func)
end
